import { createPokemonTextIndex } from '../importers/pokeapi/textsIndex'

// You have to enable this manually, to not overload the PokeAPI's servers every time we run the build:
// importPokedexes()
// importPokemonTexts()
createPokemonTextIndex()
